# P1-R 적대 교차검증

검증일: 2026-07-07 KST  
범위: `schemas/*`, `bin/vf`, `src/gates/*`, `fixtures/negative/*`, `fixtures/golden-specs/*`, `fixtures/presets/*`, `src/pipeline/deck-adapter.mjs`, `docs/contracts.md`, `docs/deck-adapter.md`

요약:
- 치명: 1건
- 중대: 8건
- 보통: 1건
- 전체 52개 네거티브 픽스처는 현재 Ajv 라우터에서 모두 실패함: `total=52`, `unexpectedPass=0`.
- 별도 제작한 적대 입력 7개 중 7개가 Ajv를 통과했다. 이 중 계약 위반으로 판단한 항목을 아래 발견에 반영했다.
- `durationFramesToMs`의 29.97/30000/1001 fps 왕복은 100,000프레임까지 실패 없음. 이 항목은 발견으로 잡지 않았다.

| 심각도 | 파일 | 결함 | 실증(실제 실행 결과) | 수정안 |
|---|---|---|---|---|
| 치명 | `bin/vf:131`, `bin/vf:135`, `bin/vf:189`, `bin/vf:210`, `src/gates/registry.mjs:7` | 게이트 리포트 신뢰 경계가 얕다. C1은 해시 재산출, pass 강제, freshness 검증을 요구하지만 현재 CLI는 생성만 하고, 필드 검사는 존재 여부뿐이다. 기존 `reports/l0-*.json`도 현재 `HEAD`와 다른 `gitCommit`을 담은 채 남아 있었다. | 실행 결과: `git rev-parse HEAD`는 `187834c9421641c803a6377c0afc5dabcb536ff0`; `reports/l0-1-report.json gitCommit=d05318edbbe4b7807b91a6b21243d50837649a49`. 필드 검사 재현 결과: `shallowPresenceCheckAccepts: true` for `{gate:"forged", pass:true, checks:[], ...}`. | `vf gate verify <report>`를 추가해 `canonicalInputMerkleHash`, `evidenceHash`, `gateScriptHash`, `gitCommit`, timestamp freshness, 필드 타입/형식을 재산출 검증한다. CI와 pre-commit은 생성된 report가 아니라 verifier 결과를 신뢰하게 한다. |
| 중대 | `schemas/audio-meta.schema.json:23`, `schemas/audio-meta.schema.json:58`, `src/gates/registry.mjs:181` | `audio_meta.words[]`의 `end >= start`, 단조 증가, `end <= audioDurationSec`가 스키마와 `validateJsonForSchema` 경로에서 빠져 있다. 주석은 런타임 체크라고 하지만 해당 검증 게이트가 없다. | 적대 입력 Ajv 결과: `audio word end before start -> ajvPass: true`; `audio word exceeds duration -> ajvPass: true`. 기존 네거티브는 음수 start/end만 잡는다. | `validateAudioMetaSemantics`를 추가하고 `vf write`, 네이티브 L0 게이트, 네거티브 픽스처에 연결한다. 씬별 `words[i].start >= words[i-1].end`, `end >= start`, 마지막 word end가 duration 이하인지 검사한다. |
| 중대 | `schemas/versions.schema.json:61` | `versions.resources[type].selected`가 `entries[].gen`에 존재하지 않아도 통과한다. `contracts.md`의 selected 단일 상태원천 규칙이 깨진다. | 적대 입력 Ajv 결과: `versions selected missing entry -> ajvPass: true` with `selected:"gen_02"` and only `gen_01` in entries. | `versions` 의미 검증기를 추가해 selected가 `null`이 아니면 같은 resource의 entries 안에 존재해야 하게 한다. 가능하면 entries를 배열 대신 gen-keyed map으로 바꾸거나, L0 게이트에서 포인터 해소를 필수화한다. |
| 중대 | `schemas/scene-specs.schema.json:18`, `schemas/scene-specs.schema.json:181` | `scene_specs`의 sceneId 중복과 dangling transition이 모두 통과한다. 안정 `sceneId`와 엣지 전환 계약을 깨뜨려 컴파일러와 deck adapter가 같은 id를 여러 자산으로 해석할 수 있다. | 적대 입력 Ajv 결과: `scene duplicate ids -> ajvPass: true`; `scene transition dangling target -> ajvPass: true` for transition `{from:"s01", to:"s99"}`. | scene graph 의미 검증기를 추가한다. `sceneId` 유일성, `sceneNumber` 유일/정렬, transition의 `from`/`to`가 실제 sceneId에 존재하는지, 필요하면 인접 edge인지 검사한다. |
| 중대 | `schemas/render-manifest.schema.json:87`, `schemas/render-manifest.schema.json:183` | render-manifest의 굽힌 timing 값이 계약과 맞는지 검증하지 않는다. `durationFrames`가 `ceil(audioDurationSec * fps)`와 달라도, subtitle `endSec < startSec`여도 Ajv가 통과한다. | 적대 입력 Ajv 결과: `render durationFrames not ceil(audioDurationSec*fps) -> ajvPass: true`; `render subtitle end before start -> ajvPass: true`. | render-manifest 의미 검증기를 추가한다. `durationFrames === Math.ceil(audioDurationSec * meta.fps)`, subtitle/word 구간의 `start <= end`, scene duration 범위 내 포함을 검사한다. |
| 중대 | `schemas/render-manifest.schema.json:13`, `src/pipeline/deck-adapter.mjs:112` | render sceneId 중복을 막지 않고 deck adapter가 중복 `assets[].id`를 그대로 배출한다. deck 바인딩의 id 기반 참조가 충돌한다. | 실행 결과: 합성 render-manifest 검증 `renderSchemaPass: true`; adapter 출력 `assetIds: ["s01","s01"]`, `assetPaths: ["clips/s01.mp4","clips/s01b.mp4"]`. | render-manifest 의미 검증과 adapter 양쪽에서 `sceneId` 유일성을 강제한다. adapter는 중복 id 발견 시 즉시 실패해야 한다. |
| 중대 | `bin/vf:323`, `bin/vf:353` | `vf write`가 target을 repoRoot 내부로 제한하지 않는다. 유효한 JSON이면 `../*.json` 경로로 워크스페이스 밖 파일을 원자 쓰기할 수 있다. | 경로 해석 실행 결과: `../vf-escape.json -> /home/seunghyeong/vf-escape.json inside=false`. 안전 확인용 실패 실행: `printf '{}' | node bin/vf write ../vf-escape.json --schema scene-specs`는 schema validation에서 실패했고, `test -e /home/seunghyeong/vf-escape.json` 결과 `exit:1`. | `const abs = path.resolve(repoRoot, file)` 직후 `abs === repoRoot || abs.startsWith(repoRoot + path.sep)`를 강제한다. 추가로 계약 파일 허용 디렉터리 allowlist를 둔다. |
| 중대 | `fixtures/golden-specs/full-8types/scene_specs.json:20`, `fixtures/golden-specs/full-8types/scene_specs.json:21`, `fixtures/golden-specs/full-8types/audio_meta.json:6` | 골든 픽스처가 P2 컴파일러 분기를 충분히 대표하지 못한다. 전체 골든 14씬 기준 `visual_kind`는 `none:10`, `chart:4`뿐이고 `generate_image/search_image/map_scene/video`, `imageAsset`, `kenBurns.enabled=true`, `overrides`, `ost`가 전부 0건이다. 오디오 타이밍도 전 씬 첫 word start `0.16`, 마지막 slack `0.42`, inter-word gap이 `0.08/0.2`뿐이다. | 집계 실행 결과: `kenBurnsEnabled: {"false":14}`, `imageAssetScenes: []`, `ostPresent: 0`, `overridesPresent: 0`; full-8types 오디오 `providers: {"mock-tts:ko-KR-fixture-neutral":8}`, `firstWordStarts` all `0.16`, `lastWordEndSlackSec` all `0.42`. | P2 전에 골든을 확장한다. 이미지 생성/검색/지도/비디오, `imageAsset.placement` 전 종류, 켄번즈 방향/줌, `overrides.byFormat`, `ost`, `spotlight/parallel` reveal, 실제 또는 더 다양한 mock word timing을 포함한다. |
| 중대 | `src/pipeline/deck-adapter.mjs:91`, `docs/deck-adapter.md:40`, `research/10-reverify/deck-factory-interface.md:21` | deck-factory 인터페이스 수정 5건 중 토큰 매핑 최소 3축 요구가 미충족이다. 현재 adapter는 배경 HEX 하나만 `tokensRef` 문자열에 넣고 palette-role map과 fonts mapping을 배출하지 않는다. | 실행 결과: `renderManifestToMotionManifest` 출력 asset은 `tokensRef: "deck-tokens:inline-background:#aabbcc"`만 포함했다. 문서도 `palette role map`과 `fonts three-axis mapping`을 `not emitted yet`로 명시한다. | deck-token 산출물을 별도 JSON으로 만들거나 `tokensRef`가 실제 deck tokens 파일을 가리키게 한다. 최소 palette role, background HEX, display/body/mono font pair를 모두 생성하고 fixture로 검증한다. |
| 중대 | `src/pipeline/deck-adapter.mjs:38`, `src/pipeline/deck-adapter.mjs:118`, `research/10-reverify/deck-factory-interface.md:21` | altText 존재 확인은 구현됐지만 render scene 전체와 scene_specs의 cardinality/coverage는 검사하지 않는다. scene_specs에 없는 render scene은 실패하지만, 중복 render scene은 위 발견처럼 중복 asset으로 나가고, scene_specs의 미사용 scene은 조용히 무시된다. | 실행 결과: missing altText 케이스는 `missing altText for sceneId s01`로 실패했다. 반면 중복 render scene 케이스는 `assetIds: ["s01","s01"]`로 성공했다. | adapter 입력 검증을 강화한다. render sceneId set과 scene_specs sceneId set의 예상 관계를 명시하고, 중복/누락/초과를 모두 실패 처리한다. |
| 보통 | `src/gates/registry.mjs:137` | `--schema auto` 라우팅 regex가 너무 넓다. schema 파일이나 복합 이름도 계약 데이터 파일로 오인한다. | 실행 결과: `scene_specs.schema.json -> scene-specs`; `scene_specs_audio_meta.json -> scene-specs`; `motion-manifest.json -> ERROR cannot infer schema...`. | 정확한 basename allowlist만 허용한다. 예: `scene_specs.json`, `scene-specs.json`만 자동 추론하고, `.schema.json`이나 복합 suffix는 `--schema` 명시를 요구한다. |

검증했지만 발견 제외:
- 29.97fps, `30000/1001`, 23.976, `24000/1001`, 59.94, `60000/1001`에서 `durationFramesToMs` 왕복을 100,000프레임까지 실행했고 `firstFailure: null`이었다.
- 요청상 보고서 외 파일 쓰기를 금지했기 때문에 crash 중 partial write, symlink race 같은 TOCTOU 재현은 수행하지 않았고 발견으로 계산하지 않았다. 확인된 `vf write` 결함은 repo 밖 경로 해석 가능성이다.
