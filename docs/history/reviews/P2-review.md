# P2-R 적대 교차검증

검증 기준: 저장소 산출물은 이 파일만 작성했다. 임시 scene_specs/build/snapshot은 `/tmp/p2-review-*` 아래에서 생성했다.

## 발견

| 심각도 | 파일 | 결함 | 실증(실행 결과) | 수정안 |
|---|---|---|---|---|
| 치명 | `src/compiler/timing.mjs:32`, `src/compiler/timing.mjs:58`, `src/compiler/transitions.mjs:94`, `src/compiler/compiler.mjs:628` | 전환을 incoming startFrame에서 시작하고 outgoing slot만 `duration+transition`으로 늘려, outgoing sub-composition이 이미 끝난 뒤 slide/crossfade가 진행된다. 짧은 씬/긴 전환/29.97fps에서 빈 화면이 발생한다. | `/tmp/p2-review-timing` 실제 spec 컴파일: `--fps 29.97`, s01=10f, s02=6f, 전환=30f/24f, `totalFrames=40` vs 순수 scene sum `28`. `hyperframes snapshot` 결과 0.5s near-white 85.6%, 1.3s near-white 100.0%. 기존 `full-8types`도 9.1s near-white 100.0%, 20.6s 99.0%. | transitionFrames를 양 adjacent scene duration 이하로 clamp하고, 전환 시작을 `toStartFrame - transitionFrames`로 배치해 outgoing이 살아있는 구간에서 겹치게 한다. 의도된 tail이 아니면 `totalFrames`에 전환 tail을 더하지 않는다. |
| 치명 | `blocks/bar/block.html:315`, `blocks/pie/block.html:361`, `blocks/list/block.html:324`, `blocks/numbered/block.html:442`, `blocks/quote/block.html:460` | 5개 블록은 timeline 등록 후 HyperFrames runtime rebind/ready 신호를 보내지 않아 nested render에서 애니메이션이 바인딩되지 않는다. `compare/line/statistic`에만 들어간 ready 패턴이 누락됐다. | `hyperframes snapshot fixtures/golden-specs/full-8types/build --at 2.467,7.017,11.317,15.467,18.967,22.567,26.633,31.117`: bar/list/numbered/quote는 배경+자막만 보이고, pie는 거대 검은 원만 렌더됨. `rg` 확인: 해당 5개는 `__hfForceTimelineRebind`, `__hfFlushSync`, `__renderReady` 없음; 3개 수정 블록은 있음. | 8개 블록 모두 동일한 `timelineIds + globalThis.__timelines + __hfForceTimelineRebind/__hfFlushSync + __playerReady/__renderReady` 패턴으로 통일하고, mid-scene PNG snapshot gate를 추가한다. |
| 중대 | `src/compiler/compiler.mjs:483`, `src/compiler/compiler.mjs:535`, `src/compiler/compiler.mjs:608`, `src/compiler/audio-duck.mjs:146` | 컴파일 경로가 `audio-duck.mjs`의 word-window 기반 더킹을 쓰지 않고, 항상 silent BGM과 scene 전체 duration keyframe을 만든다. 나레이션 단어가 없는 씬도 더킹되고, index에는 실제 volume tween이 없다. BGM 없는 프로젝트도 표현할 수 없다. | `/tmp/p2-review-duck-edge`: s01 `words=[]`로 컴파일 성공. manifest `duckingKeyframes`가 t=0에 `0.045`로 ducking 시작, `bgm.path="./assets/audio/bgm-silence.wav"`, `indexHasVolumeTween=false`, `data-volume="0.15"`. | compiler에서 `buildDuckingFromAudioMeta`와 `applyDuckingToIndexHtml/Manifest`를 실제로 연결한다. BGM 입력이 없으면 `bgm:null`과 `rf-bgm` 미생성을 허용하고, word window가 없는 씬은 ducking window에서 제외한다. |
| 중대 | `src/compiler/render-lint.mjs:127` | render-lint는 `hyperframes lint`와 정규식 검사만 수행한다. body-root 규칙을 통과해도 runtime script error가 있는 scene HTML이 통과한다. | `/tmp/p2-review-render-lint-bypass`에서 compiled body-root scene에 `throw new Error("p2-review-render-lint-bypass")` 삽입. `runRenderLint pass=true`, `hfLintExit=0`, `customViolations=0`; 같은 build에 `hyperframes validate --json`은 exit 1, `composition script error: s01 Error: p2-review-render-lint-bypass`. | compile gate에 `hyperframes validate --json`을 포함해 console/page script error를 fail 처리한다. 필요하면 `build/blocks/**/*.html`도 runtime validate/inspect 대상에 포함한다. |
| 중대 | `src/compiler/subtitles.mjs:172` | karaoke tokenization이 `audio_meta.words[].word` 원문을 그대로 span으로 감싸므로, word extractor가 문장부호를 포함하면 문장부호까지 현재 단어로 하이라이트된다. | `/tmp/p2-review-subtitle-edge` 실제 spec: narration `나 Supercalifragilisticexpialidocious끝, 응?`, words `["나", "Supercalifragilisticexpialidocious끝", "응?"]`. 컴파일 결과 `<span data-rf-word-index="2">응?</span>` 생성, 1.42s snapshot에서 `?`까지 karaoke highlight. `hyperframes inspect`는 0 issue라 회귀가 숨어 있다. | leading/trailing punctuation을 word span 밖 `textToken`으로 분리하고, core word 기준 매칭/하이라이트를 수행한다. 긴 단어, 1글자, `응?` 같은 punctuation 포함 word를 fixture로 고정한다. |

## 통과/보조 로그

- E2 narration 수정(full-8types): `/tmp/p2-review-e2-full8`에서 s04만 duration `116f -> 152f`; s01-s03 startFrame 불변, s05-s08 startFrame만 `+36f`, total `1007f -> 1043f`.
- 기본 `hyperframes validate fixtures/golden-specs/full-8types/build --json`은 통과했지만, `inspect --at-transitions`는 전환 경계에서 clipped/blank issue를 냈다. validate 단독으로는 P2 회귀를 잡지 못한다.
- `stress-edge` 기본 validate/inspect는 통과했다. 별도 subtitle edge fixture도 overflow는 없었으나 punctuation highlight 결함을 확인했다.
